{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Backend - Docker Compose",
      "type": "node-terminal",
      "request": "launch",
      "command": "docker compose up --build",
      "cwd": "${workspaceFolder}"
    }
  ]
}
